version: 2

jobs:

  test:
    working_directory: ~/react-emails
    docker:
      - image: sentisis/circleci-node:latest
    steps:
      - checkout
      - run: yarn install
      - run: yarn test

workflows:
  version: 2
  test-n-publish:
    jobs:
      - test

